

//function fullName(first, last) {
//    const firstName = first;
//const lastName = last;
 //   return `${firstName} ${lastName}`;
//



const fullName = function (first,last) {
    return `${first} ${last}`;
}
document.querySelector('#fullName').textContent = fullName('Israel','Mobote');